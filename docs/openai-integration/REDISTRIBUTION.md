# Redistribution and release-risk review

**Status: public binary release blocked.** A [verified local preview](LOCAL-PREVIEW.md) is a separate completion target, requiring its own exact-source acceptance and local handoff evidence—not public upload or a claim of completed redistribution clearance. This is a bounded first-party review, not independent legal/security approval. The selected first delivery is an **unsigned Windows x64 technical preview**, retaining `0.3.0-private.1` and the compatible patched Pi 0.85.1 names. Recommend an existing maintained Node 24 LTS installation; Node 25 compatibility testing is not a promise of continued upstream security support. No code-signing or account operations are planned.

The maintainer delegated autonomous in-project completion. That removes approval pauses for in-scope decisions; it does not remove exact-head review/CI/privacy/installed acceptance, authorize credential migration or active replacement, or override a refused repository operation. Freeze a tested repository/source/tag/asset-pinned manual plan before any delivery. Actions must remain provenance-only, without executable deployment.

## Vocabulary and review layers

| Term | Meaning |
| --- | --- |
| Installed package inventory | Actual package records and byte-pinned notice candidates in an integrity-verified bundle; retain every version/location |
| Notice candidate | A named license/notice or a README grant; presence does not establish applicability or cover a parent/embedded dependency |
| Notice-source revision | A commit identifying the supplied grant text; not necessarily the source commit used to build an associated npm package |
| Published-file notice | An unchanged whole notice identified by an explicitly reviewed publication URL, byte count and SHA256; no invented Git revision, live fetch or independent publisher-authentication claim |
| Host-bundled JavaScript | Dependencies incorporated into the patched CLI during its locked host build, possibly different from installed consumer versions |
| Embedded dependency | Code/data inside a vendored JS, native or WASM payload; not necessarily visible in either npm dependency tree |
| Source-lock candidate | A recorded source dependency/version; its presence alone does not establish compiled target, features or linkage |
| Embedded path label | A package/version string observed in payload bytes; useful corroboration, not independently verified package identity or complete linkage |
| Vendored-source supplement | A notice associated with explicitly pinned derivative source files, not a grant for their entire containing package |
| Declared native consumer | Byte-identified native payload associated with that component; not independently reproduced binary/source equivalence |
| Embedded-file supplement | A source file matched byte-for-byte at a pinned range inside a pinned consumer; does not cover the consumer's other code/data |
| Runtime-notice supplement | A reviewed notice association with exact runtime consumer bytes; not automatic linkage discovery or whole-binary licensing |
| Source witness | An unchanged full upstream source file delivered as notice-verification data; an excerpt's byte range, SHA256 and source Git blob are checked against it |
| Redistribution clearance | Reviewed coverage and obligations for the actual payload, including embedded works; not merely SPDX metadata or zero returned advisories |
| Release-risk disposition | Explicit resolution, exclusion or justified documented residual risk; not deleting failed evidence or calling it flaky |

Run the source-only read-only inventory from matching trusted source, after [download pin verification](RELEASE-CONTRACT.md):

    node distribution/audit-bundle.mjs C:/Pi-Bundles/verified

It verifies the whole manifested tree before reading package JSON as data. It never imports package code, installs dependencies, queries services or writes to the bundle. Its status is `inventory-only-not-release-clearance`. Relative component paths and byte hashes are reported, not authors or raw package metadata. Review output before publication; do not upload raw local receipts. Native payloads and nested archives are listed, not disassembled/extracted or cleared for release.

## Established baseline and findings

The following findings concern accepted source `9658c4b53771003fdaa2816e4b858d28b652af03`, archive `b311ee032bfc2832535efb0f63c00e8f2b989cfb3565f3bff3af2e922e447291`, manifest `15d35d10dfb88454e75803551ba8342ca2538aa70d90c907cf675f55b68c29e5`. These are **historical inspection identities**, not hashes for a subsequent source/document change or an available public binary.

1. **The package graph alone is incomplete.** The baseline has 130 installed package records. A separate same-options host-bundling inspection, with every resulting JavaScript output byte-compared against the prepared canonical host build, identified 92 incorporated npm package records. Twenty-seven name/version pairs are absent from the installed inventory. Vendored HTML-export highlight.js is 11.9.0, while the installed npm version is 10.7.3. Review each actual version, not just a top-level lock or last-name-wins map.
2. **Out-of-scope demo payload.** Both the coding-agent tarball and its installed examples contain the DOOM overlay's `doom.wasm`. Its README identifies doomgeneric/id Software provenance and a first-run shareware download. This is not part of the cohesive runtime scope. Before delivery, either deliberately exclude it from every delivered representation with tested packaging/provenance, or satisfy its separate code/data redistribution obligations. Removing only the installed example while shipping the original tarball is insufficient. Do not relabel it MIT based on the host package's metadata.
3. **Notice location is not notice coverage.** The six Pi packages use the already included `host-patches/pi-0.85.1/LICENSE.pi`; esbuild's platform package can refer to its companion package's notice. data-uri-to-buffer contains the complete MIT grant in its README. A nested dependency's LICENSE must not be credited to its parent. The corrected inventory finds 13 records without an own notice candidate: seven have the Pi/esbuild shared-notice locations above, and six require further reconciliation. AWS credential-provider-http/login/nested-clients, clipboard and its native platform package, and standardwebhooks need explicit source/version/license-scope reconciliation or supplemental notices. Preserve existing grants and attribution; do not invent copyright holders or assume all missing filenames mean missing rights.
4. **Embedded works require their own accounting.** Inspect the prebuilt clipboard Rust module, Photon WASM (including incorporated crates/fonts), esbuild's Go runtime/dependencies, QuickJS/Emscripten, jiti's bundled transforms and vendored HTML-export libraries. The npm package's SPDX field is not a complete static-link inventory. Registry source revisions help locate terms but are not reproducible binary provenance. Photon 0.3.4's recorded source revision contains a crate manifest labelled 0.3.3 and no tracked Cargo.lock at the checked root/crate locations; do not silently invent an exact binary dependency closure from current semver resolution.
5. **Advisory result is bounded.** On 2026-09-07 the public npm bulk advisory endpoint returned no advisories for 122 names /151 distinct versions covering all installed versions, observed host-bundled JS and vendored highlight.js. This supersedes an incomplete exploratory query that lost duplicate versions and omitted embedded host JS. It does not cover the native/Rust/Go/Emscripten closure, prove absence of vulnerabilities, verify all upstream advisories, or establish service entitlement. Recheck the final actual payload before delivery; no model/service calls are needed.

These findings block binary publication of the baseline. Green source checks and package-license counts must not close issue #3 or declare redistribution complete. Preserve original artifacts and failed evidence; corrections require new source-specific artifacts and exact acceptance.

## Curation implemented after the historical baseline

The canonical `distribution/payload-policy.json` explicitly pins the six original host tarballs and ten files of the DOOM example. Source-only curation runs after the unchanged integrity-locked npm installation. It moves both representations into a new sibling `<output>.build-inputs` directory **outside the delivered runtime tree**, verifying original bytes and every remaining installed file before/after. No original tarball is repacked, removed or relabelled; the consumer lock, canonical native patch and ordinary runtime files/notices remain unchanged. Unexpected/missing/changed exclusion inputs, archive candidates elsewhere, links and pre-existing retention destinations fail closed. A partial failure is preserved, not cleaned up for reuse.

The runtime manifest includes the policy digest and exact retained-file inventory, and the policy itself is packaged canonically. The builder and both real installed acceptance commands enforce the excluded example/archive absence. The archive therefore does not carry the original coding-agent tarball as a second copy of the excluded demo. The retained originals are build evidence, **not upload assets**. Package/lock JSON is retained as build provenance; the curated bundle must not be treated as an npm installation workspace.

This resolves the identified delivery representation problem through a tested recipe, not a blanket claim about other embedded works. Exact-source CI/reproduction/installed acceptance still has to pass for each changed source, and the other notice/advisory and release-risk gates remain. Historical artifacts above are unchanged and must not be re-labelled as curated artifacts.

## Current inventory and accepted supplemental notices

PR #35 passed separate source, resulting-master and actual copied-handoff acceptance at master `80b04427036b6317cdf969ae57e39d50150f3674`, with 1,999 Windows / 1,962 Ubuntu passes plus 37 explicit exclusions, full SDK/framed CLI/seven cohorts and unchanged 55 current settings frames. Six supplemental entries share four verbatim grant files; original native contributions have scoped MIT licensing. All 14,180 native/dependency records remain unchanged from accepted C. This accepts notice packaging and presence, **not redistribution clearance or subsequent working changes**. Failed predecessor `6c54f5` remains unaccepted: its reproduced checkout assertions were corrected, but its separate notification `EXECUTION_LIMIT` cause remains unconfirmed. No failed run was retried or runtime budget widened.

The preceding read-only inventory of accepted C master `da14cdc7ad3080c5f68eabd7a464a3c512417350`, manifest `a71392636ea82aeeacb2c50495a020a0f43d9e8013679a5fa5a427c272df8a75`, finds 130 installed package records, 13 without an own notice candidate, nine native/WASM payloads and zero nested archives. This is inventory, not redistribution clearance. The native list includes retained Pi platform shims as well as the Windows helper, esbuild, clipboard and two WASM payloads; do not assume Windows execution scope makes other shipped bytes irrelevant.

**Accepted notice-presence remedy, not a retroactive change to that C handoff:** `distribution/supplemental-notices.json` pins the standardwebhooks 1.1.1 package JSON, consumer-lock integrity and the unmodified `libraries/LICENSE` grant at upstream commit `b4d2c14fc5b4ccff3ff271e3b087dff812254c59`. The library grant is MIT with Svix attribution; the repository-root Apache-2.0 license is not substituted for it. The retained public registry metadata reports that source commit, and the retained repository tree's Git blob matches the grant. This establishes the reviewed notice source relationship, not byte-identical source reproduction.

**AWS shared notice:** the accepted policy also associates credential-provider-http 3.972.72, credential-provider-login 3.972.77 and nested-clients 3.997.44 with the unmodified SDK-root Apache-2.0 grant at `313813d9e1f25eb6896cf2880977f01ee7fb2556` (`v3.1048.0`). That 11,352-byte grant is byte-identical to the existing `@aws-sdk/core/LICENSE` and retains its original Amazon attribution. All three installed package declarations identify Apache-2.0 and the same SDK repository. The inspected root has no separate named NOTICE/COPYING file, and the three inspected package directories have no overriding named grant. One `LICENSE.aws-sdk` copy is shared by three byte-pinned package entries; conflicting shared-path bytes, casing or notice-source pins are refused.

This is a **notice-presence remedy, not exact npm-source provenance**. The tag's internal versions are 3.972.39, 3.972.41 and 3.997.9, not the installed versions above. The nested-clients metadata names `packages/nested-clients`, while the inspected tree places it in `packages-internal/nested-clients`. The original reader's path refusal is preserved; a continuation reused completed downloads and read only the unfinished, tree-confirmed location. These mismatches are not normalized into equivalence, and no credential-provider code or package build script was executed.

The accepted policy uses **schema version 2**, replacing the unshipped version-1 draft. Each entry explicitly distinguishes `package`, `vendored-source` and `embedded-file` scope. It also supplies the unmodified MIT grant for `clipboard-rs` 0.3.1, with its original copyright attribution. The actual clipboard wrapper README names that version. The checksum-matched crates.io source archive (`915fa0d77c59e330333562661159b14205f76a41a6e6eb59957724f938cb8203`) contains the grant and reports commit `e6f5b4e683fec943c24e9f838da9151a95114574`. Comparison with seven shipped derivative Rust files establishes source correspondence, not equality: one matches ignoring whitespace; six retain reviewed differences, including namespace, platform and formatting changes. The additional Wayland source is not included in this supplement. The Windows native consumer is byte-pinned but has not been independently reproduced. This does **not** resolve the separate wrapper/fork contribution grant or linked Rust/runtime closure.

**Photon's source lock and one embedded font have now been located.** The checksum-matched photon-rs 0.3.3 crate archive (`06e9b6d764d6656aba14f5ae43fbf48ce98ff1b1a6beb4d2e801eb0729f88773`) reports the same Git revision as the npm 0.3.4 package. Unlike the tracked repository locations previously inspected, the archive supplies `Cargo.lock`. Its original manifest differs from the retained Git file only in line endings; both originals remain intact. The lock's wasm-bindgen 0.2.95 agrees with the actual WASM producer field. Other producer fields report rustc `1.86.0-nightly (3f43b1a63 2025-01-03)` and walrus 0.22.0. These are source/metadata relationships, not independent compiler or full linked-dependency provenance.

The complete 171,676-byte `Roboto-Regular.ttf` matches the pinned source Git blob and appears exactly at byte offset 1,403,285 inside the actual Photon WASM data section. Its name table retains `Copyright 2011 Google Inc. All Rights Reserved.` and identifies Apache-2.0. The new `LICENSE.photon-roboto` supplement preserves the upstream font grant verbatim, with the font identity/attribution in the scoped policy. Verification checks the full consumer, exact embedded range, source SHA256 and source Git blob. This is not a grant for the entire Photon payload. The other source font, Roboto-Black, was not found byte-identically and is not claimed as a second embedded font.

The builder copies configured grants into `distribution/notices/`, outside upstream package directories, and validates package identity, exact package JSON bytes, locked package integrity, notice bytes and the recorded Git blob before recording the supplement in the bundle manifest. Vendored entries additionally verify their explicitly listed source and declared native-consumer bytes; embedded-file entries verify only the named byte range in the exact consumer. Supplemental license files disable Git line-ending conversion so the reviewed upstream bytes remain stable. The registry archive pin and archive-reported commit record reviewed inspection provenance; the builder does not download or independently reproduce that archive. The read-only audit verifies that record when present. Existing upstream metadata/notices stay unchanged; historical bundles without a supplemental record report `not-supplied`. A package still lacks its **own** notice even when a supplement is supplied. The separate status `supplemental-notice-presence-only` must not be presented as legal/security approval, complete embedded dependency coverage or completed redistribution clearance. Those source/master/copied-artifact gates passed for PR #35; each later changed builder still requires its own acceptance.

Exact AWS package-source provenance, the clipboard wrapper/fork's missing supplied grant, remaining embedded Rust/Go/Emscripten/font and host-bundled JavaScript coverage, current advisories, and durable manual-delivery qualification remain open. A fresh inspection of the three exact AWS registry versions confirms matching consumer-lock integrity but no `gitHead` or provenance-attestation field; no source commit is inferred. Public repository metadata identifies the clipboard wrapper as a fork of CrossCopy/clipboard; neither repository's metadata supplies a detected license. That absence alone is not a finding of absent rights, nor does the clipboard-rs grant license the entire fork. Original first-party native patch additions are now explicitly MIT-licensed via `host-patches/LICENSE` and the limited scope in `host-patches/NOTICE`, including their applied/compiled form. The builder includes both alongside upstream `LICENSE.pi`; existing Apache-2.0 components and third-party terms are unchanged. This resolves our contribution-grant ambiguity, not third-party coverage; artifact acceptance remains a separate recorded gate. Photon still lacks an established exact binary dependency closure. Do not resolve these gaps by inventing copyright holders, applying a parent notice to a differently licensed child, or treating an npm SPDX label as a complete embedded inventory.

## Further source inspection: advisories and esbuild build metadata

A public OSV query of **122 Rust candidates** (121 registry entries in the checksum/source-pinned clipboard lock plus the vendored clipboard-rs version) returned `RUSTSEC-2026-0194` and `RUSTSEC-2026-0195` for quick-xml 0.39.4. These concern quadratic attribute checking and unbounded namespace allocation; the advisories identify 0.41.0 as fixed. All source-lock paths from the clipboard root to quick-xml enter through optional wl-clipboard-rs 0.9.3, declared only for the Unix/Wayland target. That classifies an all-platform source finding; it establishes neither Windows exposure nor independent exclusion from the shipped binary. No lock was updated and no affected XML or clipboard operation was executed. Assess actual linkage, enabled features and reachable APIs rather than treating either a returned advisory or platform metadata as a complete security disposition.

The detail endpoint reports finer timestamp precision than the batch endpoint. The first strict timestamp comparison was retained as a refusal; its completed download was reused, and only the unfinished second detail request continued. The records agree at the batch's reported precision, not asserted nanosecond equality. Full response bytes and hashes remain the inspection identities.

A separate Photon review covers the newly recovered lock's **139 package candidates**: 136 new pairs were queried, while three exact pairs reused the retained clipboard query. The returned aliases resolve to **15 distinct RustSec records**, not 24 distinct vulnerabilities: eight are classified `unmaintained`, five `unsound`, and two have neither informational classification. These include imageproc 0.23.0 bounds-checking reports `RUSTSEC-2026-0116/0117` (fixed within that minor line at 0.23.1), rand 0.7.3/0.8.5 custom-logger reentrancy (`RUSTSEC-2026-0097`), bumpalo iterator lifetime (`RUSTSEC-2022-0078`), time RFC 2822 stack exhaustion (`RUSTSEC-2026-0009`) and crossbeam-epoch pointer formatting (`RUSTSEC-2026-0204`). These are source-candidate findings, not a claim that every affected API is exposed through Pi.

All source-lock paths to atty, crossbeam-epoch and serde_cbor pass through the root's dev-only criterion dependency. Other cases require feature and API review: for example, the rand report specifies enabled logging/thread-RNG features and a reentrant custom logger; wee_alloc is optional and not in Photon's declared default feature set. A lock graph does not independently prove their inclusion or exclusion from this WASM. The crossbeam advisory's title says `fmt::Pointer` while its body says `fmt::Display`. The checksum-matched crossbeam-epoch 0.9.10 source has `fmt::Pointer` implementations for `Atomic` and `Shared`, matching the title; the body wording is not used as the function identity. This resolves the naming discrepancy, not runtime reachability or binary exclusion. No advisory demonstration was executed and no dependency was silently upgraded.

A new comparison against actual WASM bytes finds 23 embedded crate/version path labels. Twenty-one match the recovered lock, including imageproc 0.23.0 and rand 0.7.3; dlmalloc 0.2.7 and hashbrown 0.15.2 are absent from that project lock. A separate bounded lookup for those two previously unqueried label candidates returned no advisories; that is not compiler/runtime coverage or a safety proof. Compiler/runtime dependency accounting therefore remains necessary, and these labels do not establish a complete independently verified binary closure. The earlier functional local-preview acceptance is not security clearance for these findings. Actual reachability, remedies or justified residual-risk dispositions remain open; passing notice tests cannot close them.

**Narrow native-call-site review:** seven Pi utilities and their source maps were checked against the accepted manifest, and 262 shipped coding-agent JavaScript files were byte-verified for dependency-import-text inventory. The reviewed image paths use Photon decoding/encoding, pixel/dimension access, flips and Lanczos resizing. The inspected Photon resize implementation calls `image::imageops::resize`, not imageproc's geometric transformations. The reviewed clipboard calls are `getText`, `setText`, `hasImage` and `getImageBinary`, not the HTML getter previously noted. This static inventory is not a complete transitive call graph, a proof about arbitrary extensions, or independent WASM linkage verification. It does not remove the need for codec/runtime review. No package was imported, image decoded, clipboard accessed or advisory demonstration executed during this inspection.

The pinned esbuild 0.28.1 release `Makefile` at `bb9db84c02433fbe37b3509f53f9f3e3cc48725e` explicitly builds a **custom Go compiler with the linker call to `ctxt.buildinfo()` disabled**. Its `go.version` declares 1.26.4, matching the previously observed binary string. The corresponding Go release source still uses the expected build-info marker. The absent marker is therefore consistent with the published release recipe; repeating the failed marker search or using an ordinary compiler build is not a provenance remedy. This is source-recipe evidence, not independent reproduction of the installed executable. The upstream recipe's general dismissal of scanner findings is not adopted here: actual dependency/version/reachability review remains required, including Go runtime and module notices.

## QuickJS notice and source-pointer reconciliation

The accepted `@jitl/quickjs-wasmfile-release-sync` 0.32.0 package already contains a **combined 2,263-byte LICENSE**, SHA-256 `446a69ae534aa8612d8b16d3bfc670c5a5050597dca3ce9820441a60d5d04fbb`. It matches the variant source blob `92facdd2f396363053ed7f052d08ea9b2ff89148` at the retained registry-reported revision `df4efb9ef2cb25c417ecb57986da462d11b244ed`; retained registry integrity matches the actual consumer lock. Its wrapper grant matches the shorter repository-root grant byte-for-byte within the combined file. Its separate 1,130-byte QuickJS core grant matches `vendor/quickjs/LICENSE`, blob `2cf449dd3eea4291da7470bd322211b61981ccd8`, including Fabrice Bellard and Charlie Gordon attribution. A comparison with only the repository-root LICENSE therefore differs by design; do not discard the core grant or add a duplicate based on that mismatch. This composition check does not cover every source-file attribution: the actual `quickjs.c` header carries 2017–2025 Bellard/Gordon dates, newer than the root grant's 2017–2021 dates. The working policy now adds eleven exact per-file core comment notices with unchanged full-source witnesses, including that newer attribution. This is conservative source-comment coverage, not complete wrapper/generated-data licensing closure.

The corresponding [variant Makefile](https://github.com/justjake/quickjs-emscripten/blob/df4efb9ef2cb25c417ecb57986da462d11b244ed/packages/variant-quickjs-wasmfile-release-sync/Makefile), blob `d5110712a2adbc7b660c299bf31da1ab0e56a50a`, selects Emscripten **5.0.1**, the Bellard `quickjs` source family and the separately checked vendor VERSION **2025-09-13**. It lists the QuickJS objects, `-Oz`, LTO, Closure processing and disabled filesystem options. Recipe settings are source facts, not evidence that an independently reproduced toolchain produced the installed bytes, nor changes to Pi's evaluation limits.

Bounded static framing found **no custom sections** in the actual 503,134-byte WASM (`105c3bed22d457e43e3d1c3c1c6959fda62a8fe06f0fc8a985303c3a2be72232`), hence no producer section to use for compiler identification. This absence does not prove a particular stripping process. That static inspection imported no package and executed neither WASM nor compiler. A subsequent, separate developer build reproduced these exact WASM bytes as described below; wrapper/core grant presence alone did not establish that result. Emscripten/libc/runtime notice coverage and advisory disposition remain required before redistribution clearance. A separate bounded registry lookup for previously unretained provenance fields reported one registry signature and no `dist.attestations` field for this exact version. The signature has not been independently verified here; its presence is not a build attestation or compiler/runtime linkage proof.

The Emscripten 5.0.1 tag resolves to `8c5f43157a3f069ade75876e23061330521eabde`. Its [root LICENSE](https://github.com/emscripten-core/emscripten/blob/8c5f43157a3f069ade75876e23061330521eabde/LICENSE) offers MIT or University of Illinois/NCSA terms and separately identifies Node.js portions and musl. The pinned [musl COPYRIGHT](https://github.com/emscripten-core/emscripten/blob/8c5f43157a3f069ade75876e23061330521eabde/system/lib/libc/musl/COPYRIGHT) includes its MIT grant, file-specific third-party terms and a narrowly defined public-header/CRT attribution exception—not an exception for the entire libc. Use of Closure as a build tool does not by itself establish that the compiler is redistributed or identify any generated runtime helpers. No compiler/SDK was installed or executed for that source-license inspection.

### Separate isolated WASM reproduction

A subsequent Windows developer build used the official Emscripten SDK 5.0.1 release mapping at SDK commit `14c18b569f55138fe4963924162244251f454fb0`, release build `bf32ae8b61ac8efeb7eca01b54c8307f992724f7`. Its 625,943,568-byte Windows archive was acquired from the official storage object at generation `1771014381360795`, checked against the publisher MD5, and locally SHA-256 pinned as `68765c3eca68b715772cf84963371448480d034cfbbcdc54dc7379786cb4e71c`. This records official HTTPS/object/checksum provenance, not an independently published SHA-256, verified signature or rebuilt compiler.

All 26 source inputs were pinned to the QuickJS revision above, including four reused inputs. The CJS recipe was expanded into explicit argument arrays; the unchanged generator bodies used built-in TypeScript stripping, a standard-library filesystem adapter and explicit root/entry selection instead of installing its development dependencies. The first attempt compiled all seven wrapper/QuickJS objects but failed during system-library creation with Windows `WinError 206` (command length). The corrected, fresh link scope reused those seven objects and generated symbol lists; `EMCC_CORES=2` reduced SDK input batches while `BINARYEN_CORES=1` remained. The failure and intermediate files were retained, not overwritten or retrospectively accepted.

The successful output equals the actual copied-preview WASM **byte-for-byte**, including the SHA-256 above. No QuickJS WASM was executed, and the active installation and accepted bundle were untouched. A separate build of the upstream MJS target reused the completed objects and system libraries, also reproducing the WASM. Its raw Windows JavaScript output is 10,233 bytes and differs from the shipped 10,208-byte MJS only by 25 CRLF sequences. An explicitly derived LF-emission file equals the shipped MJS exactly (`3b0757e4b1051a5f344322ad6a02c075dadff70cdb50d37b7df6c120d4af77e9`). Raw outputs, upstream grants and accepted artifacts remain unchanged; this is not a claim that the raw Windows JavaScript output was byte-identical. Profiles, Git/npm configuration, temporary files and compiler cache were isolated before compiler execution. Developer acquisition/building is not invocation-time compilation or a production fallback; no global SDK, Docker, model-service or authentication change was used.

The retained link includes `libc_optz`, `libnoexit`, `libc`, `libdlmalloc`, `libcompiler_rt` and `libsockets` archives. A separate diagnostic link, with map/extraction reporting enabled and no source recompilation, reproduced the pre-optimization WASM exactly. It reports 321 extracted members: seven from `libc_optz`, one from `libnoexit`, 294 from `libc`, two from `libdlmalloc` and 17 from `libcompiler_rt`; no `libsockets` archive member was selected in that diagnostic link. Extraction is not proof that every member survives final optimization, nor a general no-network guarantee. The SDK's root and musl grant bytes match the pinned source texts; `compiler-rt/LICENSE.TXT` separately includes Apache-2.0 with LLVM exceptions and legacy terms, and dlmalloc identifies its public-domain/CC0 origin. Source-basename mapping initially found 313 unique candidates, seven ambiguous names and one collision-renamed member. Reconciliation against retained compiler argument records now maps all 321 extracted members to observed translation units, including Windows' `_Exit`/`_exit` object-name collision. The first log reader missed the compiler's `executed` wording, and a subsequent reader omitted attached `-o` arguments; both incomplete outputs remain preserved. This is translation-unit correlation, not an included-header graph or final-code proof. The inspected musl math headers contain additional Sun, Arm, BSD and Moshier terms; the root COPYRIGHT's description of permissive third-party terms does not replace their required notices. Remaining source/header accounting, per-file terms and advisory disposition remain open. The initial runtime-notice delivery implementation below is separate from accepted master80. WASM reproduction does not by itself establish redistribution clearance.

### Runtime-notice delivery policy — accepted baseline and working follow-up

PR #36 passed separate exact-source, resulting-master and actual copied-handoff acceptance at `d95305043c9bc1afd9e979965e6687448bfe44be` (reviewed source `40bd8046ed99d56085580dddba87ca14b705bfcc`): 2,037 Windows / 2,000 Ubuntu passes plus 37 exclusions, full SDK/framed CLI/seven cohorts and all 55 settings frames. Its COMMENTED AI-assisted self-review is not independent approval. That checkpoint certifies the 52-entry notice-presence baseline, not the later working changes below, complete linkage or redistribution clearance.

Schema **v3** adds `runtime-notice` while continuing to read v2 policies. The accepted baseline has 52 entries: the six PR #35 entries are unchanged, and 46 runtime associations retain the Emscripten root LICENSE, musl COPYRIGHT, compiler-rt LICENSE (including LLVM exceptions and legacy terms), and 43 distinct copyright-bearing comment blocks (23 system translation-unit, nine system-header and eleven QuickJS core source/header blocks). These preserve Sun/SunSoft/SunPro, Arm, BSD, Moshier and Emscripten attribution/terms verbatim rather than assuming that musl's root description replaces them. Arm's SPDX comments accompany the complete musl MIT grant. The existing combined QuickJS wrapper/core license is not replaced or duplicated.

Four bounded, read-only public directory-metadata requests at Emscripten commit `8c5f43157a3f069ade75876e23061330521eabde` bound 67 SDK source/notice files to repository Git blobs. Three subsequent requests, reusing two prior directory responses, bound nine header witnesses plus the WASI API header and dlmalloc source. The two previously downloaded root grants were reused. This verifies those source-text identities, not compiler reproduction or complete dependency closure.

A separate dependency-only Clang scan completed **325 observed compile configurations** covering the 321 selected system-library translation units. It produced no objects or WASM and executed no guest code. Its 510 unique input dependencies include **189 additional headers**, all byte-matching original SDK files; all 7,399 frozen source/header/compiler inputs remained unchanged. Relative source arguments were mapped to their pinned SDK locations after an initial reader incorrectly resolved them against the repository cwd. The failed preparation and path diagnosis remain retained. Original preprocessing-relevant flags were retained; default configuration discovery was disabled and unused-command-line warnings suppressed for this diagnostic-only operation. This is the union of observed system-library configurations, not a claim about final optimized reachability, the seven wrapper/core units or generated JavaScript helpers.

The header review identified nine additional exact notice blocks, now supplied with witnesses. It also identified LLVM/Clang header license references and the WASI API header's explicit CC0 origin. Clang's resource-directory label is 23; it must not be substituted for compiler-rt's separate source-version information. The block/line classifier is an inspection aid, not a complete license parser. Further helper/source and advisory accounting remains necessary.

`coverage.sourceRelease` names the source distribution (`emscripten-5.0.1`), not a fabricated independent musl/compiler-rt version. `coverage.consumers` pins the actual QuickJS WASM; the Emscripten root association additionally pins its MJS. `noticeSource` is either `whole-file`, retaining the ordinary direct notice/Git-blob check, or `excerpt`. An excerpt carries an offset into an unchanged full source witness under `distribution/notices/LICENSE.source-*`; verification checks the witness's size, SHA256 and Git blob plus exact excerpt bytes. These 32 source witnesses are delivered as data, not compiled or imported. The small source-file overhead enables offline verification of the full source blob without normalizing notices or embedding private compiler logs. Required copyright attribution is retained; the selected witnesses have no additional publication findings outside their notice ranges.

The builder obtains the policy's unique notices and witnesses from canonical Git source through `supplementalNoticeFiles()` and verifies the resulting manifest record: 50 notices/43 witnesses at the accepted baseline, 58 notices/49 witnesses in the working v4 follow-up below. The unchanged Emscripten root grant contains one space-only line and a final blank line; file-specific blank-at-EOL/EOF attribute exceptions retain those upstream bytes rather than trimming them. The first expanded checkout check caught the EOF condition and remains a retained failure; its Git helper now includes stdout in failure diagnostics. A subsequent header-witness checkout caught two original trailing spaces in `emscripten.h` documentation. That Emscripten source witness individually exempts blank-at-EOL checking, preserving its exact upstream bytes; the failed check is retained. Five newly supplied QuickJS witnesses also need explicit file-specific whitespace exceptions: `cutils`, `engine-header`, `engine` and `regexp` retain trailing whitespace, while `libc` retains trailing whitespace and a final blank line. No wildcard exception is added. The checkout test checks the exact per-file attribute map and requires default whitespace checks for every other delivered notice/witness. The isolated Git/CRLF checkout regression covers all notices/witnesses and cached whitespace checks. Witness names are reserved in v3; conflicting shared pins, case aliases, foreign consumers, altered ranges or hidden provenance/clearance fields fail closed. Existing limits remain: 128KiB policy, 64 entries, 64KiB per notice, 2MiB per witness, 16MiB per consumer and 512 component files/32MiB aggregate. This remains `supplemental-notice-presence-only`, not trust in an arbitrary unverified directory or independently authenticated upstream metadata.

No accepted preview, native package, helper or lock is changed by the working follow-up. Focused policy and actual-byte development checks do not replace new exact-source review/CI, resulting-master and actual-copy acceptance. Remaining wrapper/header/generated-helper terms, dlmalloc and WASI/LLVM source-scope accounting, other runtime obligations and advisory qualification remain open; there is no whole-binary or public-redistribution clearance.

#### Generated JavaScript comment supplements — working, not accepted

The JavaScript follow-up adds **six associations**, preserving all preceding 52 entries; the two published Unicode notices below bring the current total to **60 entries, 58 notices and 49 witnesses**. The six JavaScript associations retain the six distinct Emscripten copyright-year comments observed in the retained MJS intermediate: 2010, 2015, 2019, 2020, 2023 and 2024. Each exact comment has a full-source witness from a corresponding `src/` file at Emscripten commit `8c5f43157a3f069ade75876e23061330521eabde`; a single bounded public directory-metadata read matched all 14 observed SDK include sources to their upstream Git blobs and original SDK inventory bytes. The six selected witnesses require no new whitespace exceptions. Their 12 additional assets total 58,858 bytes and remain outside installed packages. The existing full Emscripten root grant accompanies these SPDX comments unchanged; the comments do not replace it.

The retained Closure, modularized and JS-mangling intermediates preserve these six attributions, whereas the final raw MJS and byte-matched shipped MJS contain none of those comments. The SDK's [`handle_license()`](https://github.com/emscripten-core/emscripten/blob/8c5f43157a3f069ade75876e23061330521eabde/tools/js_manipulation.py) removes matching Emscripten comments and conditionally prepends a generic 2010 header; `tools/link.py` calls it during finalization. [`src/settings.js`](https://github.com/emscripten-core/emscripten/blob/8c5f43157a3f069ade75876e23061330521eabde/src/settings.js) defaults `EMIT_EMSCRIPTEN_LICENSE` to false, and the retained expanded recipe has no explicit override. This is static source and retained-output evidence consistent with that mechanism, not a new instrumented build or a claim of absent licensing rights. No compiler, generator, SDK module or guest was executed for this follow-up, and originals were not normalized or overwritten.

Each new association names **only** the exact 10,208-byte MJS consumer (`3b0757e4b1051a5f344322ad6a02c075dadff70cdb50d37b7df6c120d4af77e9`), not the WASM. Include markers and preserved comments do not prove that a source file's executable code survives optimization: several included sections contain only comments in this configuration. These six distinct notices are therefore conservative attribution coverage, not a complete generated-JS library/helper inventory, memory-profiler/debug-feature enablement or final-code reachability proof.

The byte-pinned `libunicode-table.h` separately labels itself generated and occurs in the retained `libunicode` preprocessing dependencies. Its header alone does not establish the Unicode data version or grant. The following source-recipe and published-notice findings narrow this gap without reproducing the data generator or proving the table's complete data provenance.

#### Published Unicode notices — working schema v4, not accepted

The pinned QuickJS [`unicode_download.sh`](https://github.com/justjake/quickjs-emscripten/blob/df4efb9ef2cb25c417ecb57986da462d11b244ed/vendor/quickjs/unicode_download.sh) selects **17.0.0**. Static inspection of `unicode_gen.c` identifies twelve file inputs on its output-generation path; `NormalizationTest.txt` belongs to the optional self-test path, and the download recipe's `PropertyValueAliases.txt` is not named by that generator. The Makefile runs `unicode_gen` only when its local database files are available. These scripts were read as data, not executed; the supplied table was not regenerated. The recipe-selected version is not proof of complete table equivalence or final optimized reachability.

Unicode's published [17.0.0 UCD ReadMe](https://www.unicode.org/Public/17.0.0/ucd/ReadMe.txt) identifies final data, publication date 2025-08-15 and 2025 Unicode attribution. Its original 740 bytes have SHA256 `9fe1a90bd32659d7953616283dc2bffaa165518aae9ace026040c42c559ba606`. The separately retrieved [Unicode License V3](https://www.unicode.org/license.txt) grants data/software use with its notice in copies or associated documentation; its original 1,995 bytes have SHA256 `e7a93b009565cfce55919a381437ac4db883e9da2126fa28b91d12732bc53d96` and retain 1991–2026 attribution. The URL is not an immutable version identifier: these are reviewed snapshot bytes, not automatic permission to adopt future responses.

The tools repository is **not a byte-identical substitute**. At `unicode-org/unicodetools` revision `a044d3b00280ae04f9915e8eeced2e4701709d99`, its root LICENSE instead has 2001–2024 attribution and an appended SPDX identifier; the remaining grant text matches. Its nominal 17.0.0 ReadMe is a template containing `PUB_DATE`, `COPY_YEAR`, `PUB_STATUS` and `UNI_VER`. The [repository README](https://github.com/unicode-org/unicodetools/blob/a044d3b00280ae04f9915e8eeced2e4701709d99/README.md) explicitly directs production users to published unicode.org data, not repository data. Both failed exact-match comparisons and all originals remain retained. No repository template, rewritten copyright date or fabricated commit is used to certify the published files.

**Schema v4** consequently adds only the `upstream: {kind:"published-file",url,bytes,sha256}` form for **whole-file runtime notices**. Its current URL allowlist contains exactly the two published locations above. Queries, fragments, credentials, alternate ports, lookalike hosts, other versions/paths, excerpts, package-wide scope, Git fields and authentication claims are refused. Existing Git-based entries, blob checks, v2/v3 readers, witness reservations, shared-source conflict checks and all size/count budgets remain intact. Verification is local and data-only: it never fetches a URL. Publication URL and SHA256 identify the reviewed notice snapshot; bundle integrity remains a prerequisite and neither constitutes independent source authentication.

Two additional unchanged whole-file notices (2,735 bytes) accompany only the exact WASM consumer, retaining the full published grant and final release attribution. Their `sourceRelease: unicode-data-17.0.0` denotes the published data release selected by the recipe, **not** a certificate that the consumer's tables have been reproduced from it. All preceding 58 entries stay unchanged. The current 60-entry policy has 54 runtime associations, 58 unique notices and 49 source witnesses (107 supplemental assets). No new whitespace exception, dependency/runtime change, compiler execution, guest execution or production data replacement follows. Complete data/helper provenance, obligations and advisory disposition remain open.

### QuickJS source reconciliation and bounded advisory review

A new public OSV batch query returned no records for the three actually installed npm packages at 0.32.0: `@jitl/quickjs-ffi-types`, `@jitl/quickjs-wasmfile-release-sync` and `quickjs-emscripten-core`. This is a wrapper-package result, not an embedded-engine or compiler security disposition.

Bellard's public repository returned no tags. Its retained VERSION commit history uses generic `new release` messages for recent releases, so message-based selection did not identify the requested date. The dated release candidate `fa628f8c523ecac8ce560c081411e91fcaba2d20` was then checked by actual source Git blobs: VERSION and five other supplied build inputs match, but **14 of 20 vendored files differ**. The date therefore does not establish that this upstream release is the exact engine source used in the reproduced WASM. The earlier reproduction remains evidence for the actual pinned vendored source, not this new candidate.

An OSV commit query for that candidate returned four records: `CVE-2025-12745` (array-buffer slicing over-read), `OSV-2024-204` (object-shape use-after-free), `OSV-2024-207` (bytecode branch emission overflow) and `OSV-2025-515` (property-definition uninitialized value). The first and last records identify fixed commits `c6fe5a98fd3ef3b7064e6e0145dfebfe12449fea` and `2d99c323a27ca9013230cfd6fb56ab37f2a0f1d4`; the two other returned ranges have no fixed event. These are **historical upstream-candidate findings**, not the current source baseline. A production-source comparison found the actual `js_array_buffer_slice` body byte-identical to the first fixed commit's 1,973-byte body. `JS_DefineProperty` differs from its historical fix point; that difference alone does not show a missing fix.

Further retained vendor history identifies update `91e701752ed38b495550e10b44c51aae27e7d92c` and its explicit subtree revision **`f1139494d18a2053630c5ed3384a42bb70db3c53`**. This is a later upstream snapshot that still reports VERSION 2025-09-13. Nineteen of the 20 supplied build files match that revision's Git blobs exactly. The sole `quickjs.c` difference is two preprocessor guard lines disabling short-integer bytecode optimization under `__EMSCRIPTEN__`, labelled as a WebKit WASM workaround in `push_short_int`. The full, byte-pinned production diff is retained; no other source difference was discarded or normalized.

Public comparison metadata reports both historical fixed commits as ancestors of that imported subtree (25 and 68 commits ahead, with each fixed commit as merge base). This establishes the recorded fixes in the **imported ancestry**, with additional exact fixed-function evidence for the array-buffer case. Ancestry alone does not prove that every change survived later upstream edits, nor does this establish whole-engine clearance. The differing property-definition body is not by itself evidence of a missing historical fix.

An OSV query for the recorded subtree returns **six records**, with the target-specific Atomics exclusion below and five other dispositions still open: `OSV-2024-204` (object-shape use-after-free), `OSV-2024-207` (branch-emission overflow), `OSV-2025-1016` (uninitialized value in function creation), `OSV-2025-1018` (regexp stack overflow), `OSV-2025-1039` (regexp-fuzzer write finding) and `OSV-2026-703` (Atomics use-after-free). The returned ranges for `OSV-2025-1016` and `OSV-2026-703` identify later fixed commits `d7ae12ae71dfd6ab2997527d295014a8996fa0f9` and `e182e3df5c51017a7faa97a654b3f2b9c6330448`; the other returned ranges do not identify a fixed event. A missing event is not proof that no upstream fix exists. Actual runtime reachability, applicable remedies and residual-risk disposition remain open; neither WASM containment nor the small vendor delta is an automatic exclusion. No demonstration, guest program, malformed input or package was executed for this advisory inspection.

**Target-specific preprocessing evidence:** a new, bounded Clang pass reuses the seven actual wrapper/core compile configurations from the successful object build. It preserves target, defines, optimization and sysroot, replaces object generation with `-E -dD` and dependency reporting, and keeps all 9,008 frozen inputs unchanged. Both `EMSCRIPTEN` and `__EMSCRIPTEN__` are defined in every result; `CONFIG_ATOMICS`, `js_atomics_op` and `JS_AddIntrinsicAtomics` occur in none. This supports **compile-time exclusion of the reported OSV-2026-703 function for this reproduced target**. It is not a claim about every Atomics implementation, all configurations, general network capability or whole-engine security. Other named source functions (`JS_NewObjectFromShape`, `emit_goto`, `js_create_function`, `lre_exec`) remain present; mere presence is not a complete reachability or exploitability assessment.

The pass finds 102 distinct dependency paths; those dependencies plus the previously supplied build files have 109 distinct contents, all matching original SDK or supplied-source bytes. Thirty-four dependency contents are additional to the earlier system-library scan. Eleven previously undelivered copyright-bearing comment blocks have been added verbatim with full Git-blob/SHA256-bound source witnesses; these retain Bellard/Gordon year ranges, the list implementation, regexp/Unicode utilities and dtoa attribution. Their `sourceRelease` label `quickjs-wasmfile-release-sync-0.32.0` names the versioned variant distribution, not a fabricated independent engine version. Root grants and all previous 41 policy entries stay unchanged. The source-comment inventory does not resolve generated Unicode-data attribution, generated JavaScript helpers, all LLVM/WASI/allocator obligations or the remaining five advisory dispositions.

For `OSV-2025-1016`, the retained reported fixed range is the direct parent `a31dcef98c7d1d8927871ac169a1c24ad8875046` through `d7ae12ae71dfd6ab2997527d295014a8996fa0f9`. The endpoint is titled “added JSON.parse source text access”; its complete-count production diff adds 372/removes 31 lines in `quickjs.c`, with no direct hunk in `js_create_function`, and also changes an atom definition and test/configuration data. This is not an established minimal security repair. The endpoint metadata does not establish causality, rule out a layout-sensitive finding, or authorize silently adding that broad feature to the delivered engine. No patch was applied and the disposition remains open.

## Earlier synthetic shell observation

Initial PR #4 head `b5167535ae327a72cc0a8a7107a2e0a03d7e2f6d` failed installed synthetic shell readiness in run `34087953229` with nested status `error` rather than `ok`. The old assertion did not retain the bounded error code. Its cause remains unresolved. The later CRLF/canonical-license correction is a separately proven defect, not an explanation of that shell result.

The current acceptance path still requires a real returned shell's READY marker, successful cell result, automatic scope-owned teardown without another wait, and zero final active/draining scopes. Runtime limits, native supervisor admission and bounded polling remain intact. The diagnostic allowlist includes both base runtime and RPC error codes, so `TOOL_LIMIT`/`GATEWAY_FAILED` are not hidden as unclassified. A source-only observer of genuine native shell events retains bounded start/end counts, last operation/status/exit code, known termination categories and output presence—not command/ID/output/error text. It does not alter the guest program or act as ownership authority. Do not print guest output, private paths or payloads to diagnose it.

Genuine QuickJS tests of the unchanged acceptance program now distinguish immediate and final-poll readiness from three synthetic failures: launch denied, poll denied and no READY within thirty polls. All three failures yield `EXECUTION_FAILED`; this proves the old status assertion cannot distinguish these causes, not that the historical CI run took any of them. Further bounded experiments must distinguish actual shell startup/readiness failures from runtime/RPC limits using new isolated evidence. Do not replay the old head hoping for green, relax success/drain assertions, or claim a historical root cause from unrelated current success. The [local technical-preview risk disposition](LOCAL-PREVIEW.md) retains this as a disclosed reliability limitation, conditional on every new exact-source gate passing. Broader public release-risk review remains open; no historical root cause or current failure waiver is invented.

PR #5 initial head `458fbaf37198e6362d2854311cee3b0cc4944c32` also failed hosted acceptance: artifact run `34095727194` recorded `EXECUTION_FAILED` with no shell marker, and runtime run `34095727202` recorded `WALL_LIMIT` rather than the expected syntax failure. Local success did not override either failure; no same-head rerun occurred. Native event diagnostics address the remaining visibility gap. Outer runtime test files are now serialized to avoid independent stress fixtures competing for fixed watchdog windows; in-file concurrency cases, runtime limits and expected results are unchanged. The causes of these specific runs remain unresolved until evidence establishes them.

## Completion gates

- Resolve actual package and embedded notice coverage, out-of-scope archives and advisory findings; retain required attribution.
- Review each packaging/runtime change through a new feature PR and labelled COMMENTED AI-assisted self-review, not independent approval.
- Require all eight contexts and every other current-head check, matching exact-source Node 24 CI and local double builds, and real extracted SDK/profile/Fast/rollback acceptance.
- Freeze the final sanitized durable record and tested manual delivery plan under standing authorization. Existing wrappers are not to be bypassed or weakened.
- Verify actual uploaded/downloaded bytes and new isolated installed acceptance before reporting availability. The user's intended profile/login remains their separate installation action; never import active credentials.
