# Pi Coding Agent Extensions

This public source repository is not the active Pi installation. Develop and validate in isolated directories/profiles. Never import credentials, modify global settings/SDKs, or run live hosted-service checks during routine validation. Keep authenticated GitHub controls separate from SDK runners; isolate HOME/USERPROFILE/APPDATA/LOCALAPPDATA/TEMP and configuration before SDK imports.

## Current scope and navigation

- PR #32 accepted the coordinator/shell milestone; PR #33 accepted bounded Web/media. PR #34's bounded native context/notification milestone passed separate exact-source, resulting-master and actual copied-handoff acceptance at master `da14cdc7ad3080c5f68eabd7a464a3c512417350` (reviewed source `758770d56702eb2aa3555a70684684105e3fe3a4`). Its 1,930 Windows / 1,893 Ubuntu tests plus 37 platform exclusions certify that milestone only, not later trees or whole-project completion.
- Owned Web references/click/continuation remain blocked on the reference authority/lifetime contract; the pinned usage description demonstrates selectors, not an authoritative result-ID DTO. Do not replace that gap with citation parsing. Independent redistribution qualification is active: working source adds scoped standardwebhooks, shared AWS SDK, vendored clipboard-rs and byte-matched Photon/Roboto notices, not release clearance or a new accepted artifact. Remaining media/audio and broader compatibility stay open. No active installation, public binary release or independent approval follows from local acceptance.
- Start with `docs/openai-integration/README.md` for shared terms, `DELIVERY-PLAN.md` for scope, and `VALIDATION.md` for commands and acceptance boundaries. `SECURITY.md`, `FINAL-ACCEPTANCE.md`, `WEB-PROFILES.md` and `MEDIA-INPUTS.md` describe important restrictions. Read `PUBLICATION.md` before public changes.
- `openai-compatibility/` contains extension registration, capability/preferences policy, Web/image adapters, Code/Unified tools and the restricted runtime. `host-patches/pi-0.85.1/` pins the matched native patch and provenance. `distribution/` builds, verifies, installs into explicit profiles, and accepts actual bundles. `.github/scripts/` contains isolated development/validation controls.
- Keep one coherent functional batch active. Use focused checks during implementation; run full mandatory acceptance at completed milestones, not after every small edit. Do not edit maintained source or pinned controls while a source-bound worker is active.

## Native ownership and shared vocabulary

- Preserve native model/provider/auth/cache/transport ownership, ordinary tools, auto-compaction, Fast/footer/settings, hooks/events/accounting, generated originals and third-party notices. Do not replace providers or use stock-host, unrestricted/portable, invocation-time compilation or download fallbacks.
- Production Code requires Windows x64, Node 24/25, .NET Framework 4.8, matched patched Pi 0.85.1 and the verified prebuilt helper. Prefer maintained Node 24 LTS. Curated bundles are not npm workspaces.
- **Native scope/invocation** means host-owned authority. Shell IDs, provider `call_id`, native ToolCall IDs, evidence IDs and presentation metadata are distinct; lookup values do not confer authority. ID-contract changes must update native quiet-poll/grouping classification and matched artifacts together. Do not admit historical IDs to live tools.
- **Finalization, publication and RPC admission** are separate boundaries. `tools` projects recognized normal results only after the required native/evidence/RPC checks; `nativeTools` preserves raw `{result,isError}`, and `projectedTools === tools`. Hook changes, errors, loss, unknown or oversized results keep wrappers/evidence. Do not rewrite historical scripts or artifacts silently.
- **Return wait** is not process lifetime; cancellation is not confirmed closure. `wait` collects Code cells; `write_stdin` addresses numeric shell lookup IDs. Quiet polling must not hide meaningful output/errors or imply token savings. Restricted coordinator JavaScript does not sandbox deliberately delegated shell commands.
- **Notification** means primitive `await notify(value)` additional native output for the original exec across adopted waits—not a toast, completion, usage event or permission to manufacture an idle turn. Journal/queue acceptance is not consumption. Historical orphan notices remain labelled history, not invented calls.
- Code collector return is not whole native prompt finalization. Normal successful-work cohorts require zero final active/draining scopes. The separate acknowledgement-fault cohort genuinely publishes once before delaying acknowledgement and deliberately retains exactly four quarantined scopes; never describe those as cleaned up. No crash/fsync-recovery or physical-process-state claim follows.

## Context, Web and media boundaries

- `/openai-tools` is the capability entry point; `/fast` and `/openai-jobs` are not aliases. Persist preferences, not jobs/cells/credentials/references/authority. Saved and effective settings differ where reload is required; preserve malformed files for repair.
- `verified-v1` and `experimental` are context-free. `experimental-context` is an explicit reload-required conversation-disclosure opt-in, not secret scrubbing. Context requires final native hooks, actual serialized Responses bytes, successful completion and current invocation. No ancestry reconstruction, replay or context-free downgrade.
- Context bounds remain 512 messages/128 blocks/64KiB text/2MiB serialized request. Web input retains the last two native users and intervening assistant text, with 4,000 shared assistant-prefix UTF-8 bytes and an 8,192-byte serialized DTO. These are Pi restrictions, not upstream token/middle-truncation parity.
- Web admits one to four bounded operations under native/provider leases; verified-v1 stays narrow and experimental supports nine source-derived families. Current open/find/screenshot use public URLs, not opaque continuation. `results` remains opaque source evidence. Do not invent authoritative DTO fields or mint ownership from citation-looking text, copied metadata, hashes or matching strings. Native `ev_...` references are not service `ref_id` values.
- Inline PNG/JPEG/GIF/WebP inputs use own data descriptors, a 32KiB decoded cap, one declared frame, 4,096-pixel edge and 4,194,304-pixel canvas bounds. Proxy traps remain watchdog-bound; container/JPEG framing checks are not pixel decoding or a codec sandbox.
- `generatedImage()` forwards protected image content and an optional unverified output hint; it does not generate, fetch or save. Projected imagegen `image_url` is an owned Pi reference, not upstream data-URL parity. Preserve originals and warnings; never replay generation merely to recover a copy or metadata.

## Working commands and acceptance lessons

- From the verified feature-branch root, `node .github/scripts/develop-openai.mjs --list` lists focused suites. Examples: `node .github/scripts/develop-openai.mjs context types` and `node .github/scripts/develop-openai.mjs profile-receipts workflow`. Existing dependencies/helper are required; focused runs retain isolated private logs, do not build/download, replace the shared full-validation receipt, or authorize integration/delivery.
- `node .github/scripts/develop-openai.mjs notice-coverage workflow` checks supplemental-notice/inventory policy in isolation. Supplemental grants stay outside upstream package directories. One notice file may serve multiple reviewed package entries only with identical path/byte/notice-source pins; the builder copies it once. The AWS root grant matches shipped core/LICENSE, but its inspected tag has different internal package versions and a corrected nested-clients directory. Treat its commit as the notice source, not npm build provenance. Working schema v2 distinguishes package, vendored-source and embedded-file notices; it pins package/lock/notice bytes plus explicit source/consumer bytes or embedded ranges. Preserve original supplemental grant line endings via `.gitattributes`. clipboard-rs 0.3.1's MIT notice applies to original material in seven derivative files, not the whole clipboard wrapper or its additional Wayland file. Photon's registry crate supplies a source lock absent from the tracked repository; source manifest differences are line endings only. Roboto-Regular bytes match one exact WASM range; this does not cover the rest of Photon. Native consumer pins/producer fields do not prove binary/source equivalence. A supplement does not erase a missing-own-notice finding or establish complete embedded coverage/redistribution clearance. FOSS licensing decision: original first-party native patch additions (including applied/compiled form) use MIT in `host-patches/LICENSE`, scoped by `host-patches/NOTICE`; keep upstream `LICENSE.pi` and existing Apache-2.0/third-party terms unchanged. The builder must include all three notices. This resolves the contribution grant, not third-party redistribution clearance.
- esbuild's pinned release recipe deliberately disables Go linker build-info emission using a custom compiler. The missing marker is consistent with that recipe, not a reason to replay the failed search or claim corruption. Source recipe/version strings are not reproduced-binary provenance or a waiver of advisory review. See `REDISTRIBUTION.md` for the bounded Rust advisory findings and remaining linkage/notice gaps.
- Photon's recovered lock yields 15 distinct RustSec records (eight unmaintained, five unsound, two other), not 24 distinct vulnerabilities. Actual WASM path labels include imageproc 0.23.0 and rand 0.7.3; dlmalloc 0.2.7/hashbrown 0.15.2 labels are absent from the project lock. Source dev/optional-feature classification is not binary exclusion. The checksum-matched crossbeam source confirms the advisory title's Pointer trait rather than the body's Display wording. Reviewed Pi resize calls route to image::imageops, and reviewed clipboard calls omit the HTML getter; these are narrow source/manifest observations, not a complete call graph or security disposition. Keep advisory inspection data-only: no demonstration execution or system clipboard access. See `REDISTRIBUTION.md`; functional C acceptance and passing notice tests are not security clearance.
- Source-on-SDK development is not actual-artifact acceptance. The required bundle cohorts are `distribution/accept-web-media.mjs <bundle> <web|media|imagegen|web-profile>`, `accept-native-context.mjs <bundle> <context|notifications>` and `accept-notification-ack.mjs <bundle>`, plus full SDK and genuine CLI/profile gates. Use the exact commands/environment in `VALIDATION.md`.
- Hosted CLI acceptance requires the complete semantic receipt, not exit/check status. `distribution/accept-profile-framed.mjs <bundle>` supervises the genuine isolated CLI and emits ordered <9KiB frames. Reassemble every frame and validate original CLI/settings semantics; hashes provide consistency, not authority. Keep the four-MiB/300-second child limit, SDK aggregate limit, seven separately bounded cohorts and 20-minute artifact job unchanged.
- Earlier C candidates had green checks but missing full hosted CLI receipts; they remain unaccepted. Bounded framing resolved the corrected source's evidence gap without proving the suspected 64KiB log-line cause or retroactively accepting predecessors.
- Preserve exact UI migrations: C retained all 50 PR33 frames and added five context views. Disclosure must fit the existing 300-character description cap. Use the corresponding migration comparator; do not normalize away changes or reuse older 9→10/10→11 comparators for a different layout.
- Native `npm run check` can format source: snapshot first. Cross-workspace Vitest fixtures need explicit sibling-source imports and verification without workspace `dist`; compiled development output can mask resolution failures. Fresh source/build reproduction with verified reused dependencies is not a clean dependency installation. Classify generated cache changes rather than claiming unchanged bytes.
- Preserve raw failures, byte logs, profiles, snapshots, proof indexes, originals and spent-operation receipts. Never replay completed builds/probes/downloads/commits/uploads/public mutations for reassurance. Investigate unknown effects using journals and process identity, not PID alone. Preserve the prior offline-validation receipt before replacement.

## Canonical repository and integration guards

- Develop only in the fresh public repository `https://github.com/HalcyonXP/pi-coding-agent-extensions-public` (repository ID `R_kgDOUQewAQ`). Before edits, confirm worktree/common directory, exact origin, repository identity and non-personal commit identity. Folder names are not authority. Never repoint an installation/private-history checkout or import its Git history.
- Public history uses `Project Maintainers <maintainers@example.invalid>`. Never publish personal email, machine-specific paths, private records, credentials, local receipts or user artwork. Verify author/committer of every new commit before upload, including merges. No default server-side merge or automatic release/deployment from Actions.
- Require exact-head checks and a labelled COMMENTED AI-assisted self-review before guarded integration. Self-review is not independent approval; local tests do not replace hosted CI or installed-artifact acceptance. Equal trees do not certify later commits or interchangeable source-bearing archives. Preserve separate source, resulting-master and actual copied-handoff gates.
- Maintainer-local accident guards may exist under the Git common directory. When present, run preflight and use the explicitly scoped GitHub wrapper:

```powershell
$guard = Join-Path (git rev-parse --path-format=absolute --git-common-dir) 'workspace-guard/guard.mjs'
node $guard preflight --online
node $guard github pr view 1
```

- Guard verification is destination/privacy checking, not review or integration authorization. Commit hooks check identity/staged privacy; push hooks check destination, available history and pinned secret scans. The GitHub wrapper pins this repository and rejects overrides, URL selectors, raw API/admin, server-side merge, release and deployment commands. Preserve canonical guards; do not recreate retired locations or reinterpret historical ledger records as current guards.
- Fresh clones do not inherit local hooks. Establish equivalent safeguards before writes. Pinned validators require deliberate reviewed maintenance, never automatic replacement with unreviewed source. Prefer non-interactive execution and avoid unnecessary windows or interference with active applications.
